export interface DashboardStats {
  highestScoringCandidate: any; // Replace 'any' with your candidate type if available
  totalCandidates: number;
  totalInactiveCandidates: number;
  totalMatchedCandidates: number;
}
